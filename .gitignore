node_modules
dist
dist-server
.DS_Store
*.log
.diff-review/
.playwright-mcp/
test-results/
playwright-report/
__screenshots__/
.vitest-attachments/
.vitest/
*.tgz
*.tsbuildinfo

# Immutable release tarballs assembled by scripts/release.ts. Never committed — the published
# artifact's provenance is the git tag plus the registry, not a copy in the repo.
release/

# Local environment files may contain machine-specific configuration or secrets.
.env
.env.*
!.env.example

# Local Codex state.
.codex/

# Agent scratch worktrees. These are whole checkouts of this repo living inside it — never
# committable, and their stray config files confuse tools that walk the tree.
.claude/worktrees/
